# 🔄 Application Flow

This document explains the complete flow of the Canvas Drawing App from user registration to collaborative drawing.

---

## 📊 Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                        USER FLOW                                     │
└─────────────────────────────────────────────────────────────────────┘

┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Start   │────▶│  Login/  │────▶│  Room    │────▶│  Canvas  │
│          │     │ Register │     │ Selector │     │  Drawing │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
                      │                │                │
                      ▼                ▼                ▼
                ┌──────────┐    ┌──────────┐    ┌──────────┐
                │  Store   │    │  Create/ │    │  Save    │
                │  Token   │    │  Join    │    │  Shapes  │
                └──────────┘    └──────────┘    └──────────┘
```

---

## 1️⃣ Authentication Flow

### Registration
```
User                    Frontend                   Backend                  Database
 │                         │                          │                         │
 │── Fill form ──────────▶│                          │                         │
 │                         │── POST /register ──────▶│                         │
 │                         │                          │── Check existing ─────▶│
 │                         │                          │◀── User not found ─────│
 │                         │                          │── Hash password         │
 │                         │                          │── Create user ─────────▶│
 │                         │                          │◀── User created ────────│
 │                         │                          │── Generate JWT          │
 │                         │◀── Token + User ────────│                         │
 │                         │── Store in localStorage  │                         │
 │◀── Redirect to rooms ──│                          │                         │
```

### Login
```
User                    Frontend                   Backend                  Database
 │                         │                          │                         │
 │── Enter credentials ──▶│                          │                         │
 │                         │── POST /login ─────────▶│                         │
 │                         │                          │── Find user ──────────▶│
 │                         │                          │◀── User data ───────────│
 │                         │                          │── Compare password      │
 │                         │                          │── Generate JWT          │
 │                         │◀── Token + User ────────│                         │
 │                         │── Store token            │                         │
 │◀── Redirect to rooms ──│                          │                         │
```

---

## 2️⃣ Room Flow

### Create Room
```
User                    Frontend                   Backend                  Database
 │                         │                          │                         │
 │── Enter room name ────▶│                          │                         │
 │                         │── POST /rooms ─────────▶│                         │
 │                         │   (with JWT token)       │── Generate room code    │
 │                         │                          │── Create room ─────────▶│
 │                         │                          │◀── Room created ─────── │
 │                         │◀── Room + Code ─────────│                         │
 │◀── Enter canvas ───────│                          │                         │
 │                         │                          │                         │
 │── Share code "ABC123" with friends                │                         │
```

### Join Room
```
User                    Frontend                   Backend                  Database
 │                         │                          │                         │
 │── Enter room code ────▶│                          │                         │
 │                         │── GET /rooms/ABC123 ───▶│                         │
 │                         │                          │── Find room ──────────▶│
 │                         │                          │◀── Room data ───────────│
 │                         │◀── Room details ────────│                         │
 │◀── Enter canvas ───────│                          │                         │
```

---

## 3️⃣ Drawing Flow

### Load Existing Shapes
```
User                    Frontend                   Backend                  Database
 │                         │                          │                         │
 │── Enter room ─────────▶│                          │                         │
 │                         │── GET /rooms/:code/shapes│                         │
 │                         │                          │── Find shapes ─────────▶│
 │                         │                          │◀── Shape data ──────────│
 │                         │◀── Shapes array ────────│                         │
 │                         │── Load last shape        │                         │
 │                         │── Draw on canvas         │                         │
 │◀── See existing art ───│                          │                         │
```

### Draw & Save Shape
```
User                    Frontend                   Backend                  Database
 │                         │                          │                         │
 │── Mouse down ─────────▶│                          │                         │
 │── Draw on canvas ─────▶│                          │                         │
 │── Mouse up ───────────▶│                          │                         │
 │                         │── Capture canvas         │                         │
 │                         │── Convert to base64      │                         │
 │                         │── POST /rooms/:code/shapes                        │
 │                         │   { shapeId, data } ────▶│                         │
 │                         │                          │── Save shape ──────────▶│
 │                         │                          │◀── Shape saved ─────────│
 │                         │◀── Confirm ─────────────│                         │
```

### Undo Action
```
User                    Frontend                   Backend                  Database
 │                         │                          │                         │
 │── Click Undo ─────────▶│                          │                         │
 │                         │── Pop from history       │                         │
 │                         │── Restore previous       │                         │
 │                         │── DELETE /rooms/:code/shapes/:id                  │
 │                         │                          │── Delete shape ────────▶│
 │◀── See previous state ─│◀── Confirm ─────────────│◀── Deleted ─────────────│
```

### Clear Canvas
```
User                    Frontend                   Backend                  Database
 │                         │                          │                         │
 │── Click Clear ────────▶│                          │                         │
 │                         │── Clear canvas           │                         │
 │                         │── DELETE /rooms/:code/shapes                      │
 │                         │                          │── Delete all ──────────▶│
 │◀── Empty canvas ───────│◀── Confirm ─────────────│◀── All deleted ─────────│
```

---

## 4️⃣ Data Flow

### State Management
```
┌─────────────────────────────────────────────────────────────────────┐
│                         APP STATE                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   localStorage          React State           Canvas State          │
│   ┌──────────┐         ┌──────────┐          ┌──────────┐          │
│   │ JWT      │         │ user     │          │ past[]   │          │
│   │ Token    │────────▶│ room     │─────────▶│ future[] │          │
│   └──────────┘         │ auth     │          │ drawing  │          │
│                        └──────────┘          └──────────┘          │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### Request Flow
```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  React   │────▶│  Auth    │────▶│  Express │────▶│  Prisma  │
│  Component│    │  Service │     │  Routes  │     │  Client  │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
      │                │                │                │
      │                │                │                │
      ▼                ▼                ▼                ▼
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  User    │     │  HTTP    │     │  JWT     │     │  SQL     │
│  Action  │     │  Request │     │  Verify  │     │  Query   │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
```

---

## 5️⃣ Component Hierarchy

```
App
 │
 ├── AuthContainer (if not logged in)
 │    ├── LoginForm
 │    └── RegisterForm
 │
 ├── RoomSelector (if logged in, no room)
 │    ├── Join Room Input
 │    ├── Create Room Input
 │    └── My Rooms List
 │
 └── Canvas View (if in room)
      ├── Header
      │    ├── Room Code Badge
      │    ├── Leave Room Button
      │    └── Logout Button
      │
      └── CanvasAppContent
           ├── Tools Panel
           │    ├── Color Buttons
           │    ├── Size Slider
           │    ├── Undo/Redo
           │    ├── Clear
           │    └── Download
           │
           └── Canvas Element
```

---

## 6️⃣ API Authentication Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                     JWT AUTHENTICATION                               │
└─────────────────────────────────────────────────────────────────────┘

1. Login/Register
   ┌────────┐                              ┌────────┐
   │ Client │──── credentials ────────────▶│ Server │
   │        │◀─── JWT Token ──────────────│        │
   └────────┘                              └────────┘

2. Protected Request
   ┌────────┐                              ┌────────┐
   │ Client │──── Request + Bearer Token ─▶│ Server │
   │        │                              │        │
   │        │    ┌─────────────────────┐   │        │
   │        │    │ Middleware verifies │   │        │
   │        │    │ JWT and extracts    │   │        │
   │        │    │ userId              │   │        │
   │        │    └─────────────────────┘   │        │
   │        │◀─── Response ───────────────│        │
   └────────┘                              └────────┘
```

---

## 7️⃣ Error Handling

```
┌──────────────────────────────────────────────────────────────────┐
│                      ERROR SCENARIOS                              │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  401 Unauthorized ──▶ Redirect to Login                          │
│  404 Room Not Found ──▶ Show "Room not found" error              │
│  400 Bad Request ──▶ Show validation error                       │
│  500 Server Error ──▶ Show generic error message                 │
│  Network Error ──▶ Show "Connection failed" message              │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 8️⃣ Collaborative Workflow

```
┌─────────────────────────────────────────────────────────────────────┐
│                    COLLABORATION FLOW                                │
└─────────────────────────────────────────────────────────────────────┘

User A (Creator)                          User B (Joiner)
     │                                         │
     │── Creates Room ──────────────────────▶ │
     │   Gets code: ABC123                     │
     │                                         │
     │── Shares code via chat/email ─────────▶│
     │                                         │
     │                                         │── Enters code ABC123
     │                                         │── Joins Room
     │                                         │── Loads existing shapes
     │                                         │
     │◀─────────── Both can draw ─────────────│
     │                                         │
     │── Draws something                       │
     │── Saves to DB ────────────────────────▶│
     │                                         │
     │                                         │── Refreshes page
     │                                         │── Sees User A's drawing
     │                                         │
     │                                         │── Draws something
     │                                         │── Saves to DB
     │                                         │
     │── Refreshes page ◀─────────────────────│
     │── Sees User B's drawing                 │
     │                                         │
└─────────────────────────────────────────────────────────────────────┘

Note: Currently not real-time. Users need to refresh to see 
      each other's changes. WebSocket can be added for real-time sync.
```

---

## 🔮 Future Enhancements

1. **Real-time Sync** - Add WebSocket for live collaboration
2. **Cursor Sharing** - See other users' cursors
3. **User Presence** - Show who's in the room
4. **Chat** - Add in-room messaging
5. **Layers** - Support multiple drawing layers
6. **Shapes** - Add geometric shape tools
